# Google Scholar citation counts, keyed by "<scholar_userid>:<google_scholar_id>".
# Populated by bin/update_scholar_citations.py (scheduled via
# .github/workflows/update-citations.yml) using scholar_userid from
# _data/socials.yml. Consumed by _layouts/bib.liquid for citation badges on
# entries that carry a google_scholar_id field in _bibliography/papers.bib.
metadata:
  last_updated: "2000-01-01"
papers: {}
